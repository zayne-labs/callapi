{
	"extends": ["../../tsconfig.base.json", "@zayne-labs/tsconfig/bundler/next"],

	"compilerOptions": {
		"baseUrl": ".",
		"paths": {
			"@/*": ["./*"]
		}
	},

	"mdx": {
		// "checkMdx": true
	},

	"include": ["**/*.ts", "./*.ts"],
	"exclude": ["node_modules"]
}
